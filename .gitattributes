# Line endings are LF in the repository regardless of the checkout platform.
# Two files were committed with CRLF from a Windows tool, which rewrote every
# line of them in the diff and hid the four lines that had actually changed.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
*.woff2 binary
