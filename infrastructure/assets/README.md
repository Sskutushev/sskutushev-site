# Deployment assets

Files in this directory are versioned deployment inputs, not frontend static assets. The production
preflight synchronizes the resume PDF to the configured S3-compatible bucket before API rollout.
